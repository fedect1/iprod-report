import ReviewForm from './ReviewForm';

export const metadata = {
  title: 'Review',
};

export default function ReviewPage() {
    return (
      <div>
        <ReviewForm/>        
      </div>
    );
  }